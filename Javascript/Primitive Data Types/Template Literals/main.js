//1. Personal Greeting
const myName = 'Shadi';
const age = 30;
console.log(`My name is ${myName} and I am ${age} years old.`);

//2. Calculating Area
const length = 5;
const width = 3;
const area = length * width;
console.log(`The area of a rectangle with length of ${length} and width of ${width} is ${area}`);

//3. Daily Schedule
const today = 'Monday';
const task1 = 'learn Javascript';
const task2 = 'complete the weekend assignment';
const task3 = 'go to work';
console.log(`On ${today}, I will ${task1}, ${task2}, and ${task3}.`);

//4. Mixing Numbers and Strings
const numberOfMyPets = 3;
console.log(`I have ${numberOfMyPets} pets.`);

//5. Complex Expression: Area of a Triangle
const base = 5;
const height = 10;
const triangleArea = (base * height) / 2;
console.log(`The area of a triangle with base ${base} and height ${height} is ${triangleArea}.`);